KITEAI REPLACE WORKFLOW - STATUS AUDIT
========================================
Date: January 12, 2026

SECTION 1 — COMPLETED VS PLANNED
---------------------------------

| Item                                    | Status   | Details                                                          |
|-----------------------------------------|----------|------------------------------------------------------------------|
| a) REPLACE bypasses merge/repair        | DONE     | applyChatMutation with mode: 'REPLACE' in useChatMutation.ts     |
| b) Legacy REPLACE paths removed         | DONE     | handleAcceptWorkflow routes through onReplaceWorkflow only       |
| c) Structural regression guard          | DONE     | detectStructuralRegression in graphStructure.ts                  |
| d) Island prevention for CREATE/EXTEND  | DONE     | detectFloatingIsland, verifyNewNodesReachable, findOrphanNodes   |
| e) Removal of KEEP option               | NOT DONE | "Keep Existing" button still exists (line ~12388-12400)          |
| f) Confirm routes to onReplaceWorkflow  | DONE     | executeReplaceWorkflow calls applyChatMutation with mode:'REPLACE'|
| g) Dev-only logging                     | DONE     | Present in REPLACE confirm and regression handlers               |
| h) Tests                                | NOT DONE | No unit/integration tests exist                                  |


SECTION 2 — EDGE-CASE EXPANSION BEHAVIOR
-----------------------------------------

3) After first edge/fail case expansion:
   - NO LIMITER exists after first expansion
   - Repeated expansions are allowed to mutate the workflow
   - No code limits or changes behavior on subsequent expansions

4) When AI is asked to "look for other missing failure cases":
   - System is in MUTATION mode (not DISCOVERY)
   - No code differentiates between discovery and mutation
   - ~10 existing nodes being modified because there is no diff filtering or content freeze

5) No constraints exist preventing:
   - Rewriting existing nodes: NO GUARD
   - Rewiring unrelated edges: NO GUARD
   - Introducing abstract catch-all nodes: NO GUARD
   - These constraints should live in applyChatWorkflowMutation.ts (pre-merge diff validation)


SECTION 3 — PROMPT VS EXECUTION ALIGNMENT
------------------------------------------

6) Prompts used:
   a) First edge/fail case expansion: AI_WORKFLOW_EXPAND_EDGE_CASES_PROMPT
   b) Subsequent "find missing cases": AI_WORKFLOW_LIST_EDGE_CASES_PROMPT
   c) Targeted re-apply: AI_WORKFLOW_EXPAND_SELECTED_EDGE_CASES_PROMPT
   
   These differ only textually (mutation vs discovery intent) — execution ignores the distinction.

7) Reviewed prompts contain NO "improve/clean up/optimize" language.
   Over-mutation stems from execution-side behavior, not prompt wording.


SECTION 4 — PHASE BOUNDARY ENFORCEMENT
---------------------------------------

8) After workflow reaches EXPANDED_WITH_EDGE_CASES:
   - WorkflowGenState toggles to EXPANDED
   - NO explicit phase flag changes AI behavior
   - Behavior is inferred from conversation context only

9) Guards preventing multiple mutating expansions:
   - NO GUARD EXISTS
   - Users can loop APPLY indefinitely
   - This is currently unbounded


SECTION 5 — REPLACE VS CREATE CORRECTNESS
------------------------------------------

10) For REPLACE:
    - applyChatWorkflowMutation IS still called
    - BUT with mode: 'REPLACE' which disables repairs
    - REPLACE mode validates structure only, bypasses merge/repair logic
    - No runtime logs proving it is NOT called (it IS called, but in validation-only mode)

11) For CREATE/EXTEND:
    - Attachment logic: resolveAttachmentTarget, detectFloatingIsland, verifyNewNodesReachable
    - Low attachment confidence returns aborted mutations
    - Branch flattening persists because merge logic allows payloads that overwrite decision nodes


SECTION 6 — EXPECTED BEHAVIOR CONFIRMATION
-------------------------------------------

12) Statement: "After one successful edge-case expansion, further requests to identify 
    missing failure cases should default to DISCUSSION ONLY and require explicit user 
    selection before mutating the workflow."

    STATUS: FALSE

    Current behavior:
    - After one expansion, system still defaults to MUTATION
    - Without additional guards, repeated prompts can rewrite the graph
    - No phase lock prevents runaway graph rewrites


SECTION 7 — HIGHEST-RISK GAP & RECOMMENDATIONS
-----------------------------------------------

13) Highest-risk gap:
    - Lack of post-expansion lock or diff guard
    - Subsequent edge-case prompts can rewrite existing branches without constraint

    File to fix first: applyChatWorkflowMutation.ts

    Immediate fix: 
    - Enforce "discussion-only after expansion" mode
    - OR implement strict diff validator that prevents modifying existing nodes


RECOMMENDED FIXES
------------------

1. Remove "Keep Existing" button from REPLACE dialog (workflow-editor.tsx lines 12388-12400)

2. Add phase guard so edge-case prompts after first expansion require explicit opt-in 
   before mutating (KiteAIChat.tsx + mutation orchestrator)

3. Add diff validation to prevent rewriting existing nodes during expansion 
   (applyChatWorkflowMutation.ts)

4. Write regression tests for:
   - REPLACE success path
   - Regression warning modal
   - Island prevention


FILES TOUCHED IN LAST ROUND
----------------------------

- client/src/pages/workflow-editor.tsx (executeReplaceWorkflow, REPLACE dialog, regression warning)
- client/src/workflow/analysis/graphStructure.ts (NEW: structural regression detection)
- client/src/hooks/useChatMutation.ts (mode: 'REPLACE' parameter)
- client/src/workflow/mutation/applyChatWorkflowMutation.ts (island prevention already existed)
- replit.md (documentation updates)
