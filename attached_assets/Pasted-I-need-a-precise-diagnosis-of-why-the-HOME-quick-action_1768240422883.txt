I need a precise diagnosis of why the HOME quick actions
“Include edge / fail cases” and “Discuss edge cases”
are no longer appearing after baseline workflow generation.

Please answer each question with:
- the exact file(s)
- the exact condition(s)
- whether this behavior is intentional or accidental

1) Quick Action Visibility Logic
Where is the logic that determines whether HOME quick actions are rendered?
- File(s) and function(s) responsible
- What conditions must be true for these buttons to appear?
- Are diagnostics (e.g. missing edges / incomplete decisions) currently gating visibility?

2) Surface Gating
Are edge/fail quick actions currently gated behind `surface === 'project'` or similar?
- If yes, when and why was this added?
- Was HOME intentionally excluded or is this an oversight?

3) workflowGenState Dependency
Which `workflowGenState` values allow quick actions to render?
- Is `'BASELINE_GENERATED'` still sufficient?
- Is the state being immediately advanced or overwritten after baseline generation?

4) Diagnostics Suppression
If the baseline workflow includes explicit success/failure nodes,
does the diagnostics engine consider the workflow “complete”
and therefore suppress edge/fail actions?
- If yes, is that suppression intentional for HOME?
- If not, where should this logic differ between HOME vs IN-PROJECT?

5) Recent Refactor Impact
Since phases were implemented:
- Were any conditions added that prevent HOME from showing proposal refinement actions?
- Specifically check recent changes in:
  - KiteAIChat.tsx
  - getSuggestedQuickActions
  - workflow diagnostics / scoring utilities

6) Expected Product Behavior (Clarification)
Please confirm the intended behavior:
- In HOME proposal phase, should users ALWAYS be able to:
  - Include edge/fail cases
  - Discuss edge cases
as long as a draft exists?
If yes, identify what needs to change to restore this.

7) Minimal Fix Recommendation
Based on the above, what is the minimal code change to:
- Always expose edge/fail quick actions in HOME when a draft exists
- While still allowing diagnostics to influence copy or ordering (but not visibility)?

Please be explicit and point to code, not concepts.
