REPLIT INSTRUCTIONS — NOTION-STYLE WORKFLOW PRD UI REFACTOR

GOAL
Refactor the Workflow PRD UI to behave like a document (Notion / Google Docs style), not cards or forms.
This is a UI + interaction refactor ONLY. Do not change data models or AI logic.

OUT OF SCOPE
- PRD storage format
- AI generation logic
- Workflow grouping or layers
- Node graph changes

IN SCOPE
- SpecsTab UI refactor
- Inline click-to-edit behavior
- Typography-first layout
- Single reusable DocSection component

FILES TO MODIFY / ADD
Primary:
- client/src/components/panels/ProjectPanel/SpecsTab.tsx

New:
- client/src/components/docs/DocSection.tsx
- client/src/components/docs/WorkflowDocument.tsx

OPTIONAL:
- client/src/components/docs/docStyles.ts (typography helpers only)

SECTION 1 — REMOVE CARD / FORM UI
In SpecsTab.tsx, remove:
- Card components
- Borders and boxed textareas
- Edit mode layout toggles
- Any UI that resembles forms or inputs by default

Keep:
- Section order
- Section titles
- PRD content
- Regenerate button logic
- loadWorkflowPRD / saveWorkflowPRD calls

SECTION 2 — DOCUMENT LAYOUT
Replace section rendering with a document layout:

<WorkflowDocument>
  <WorkflowHeader />
  <DocSection />
  <DocSection />
  <DocSection />
</WorkflowDocument>

Rules:
- One scroll container (panel itself)
- No nested scroll areas
- Vertical flow like a document
- No visible section boundaries

SECTION 3 — WorkflowDocument COMPONENT
Create: client/src/components/docs/WorkflowDocument.tsx

Responsibilities:
- Page-like padding
- Vertical rhythm
- No logic

Skeleton:

interface WorkflowDocumentProps {
  children: React.ReactNode;
}

export function WorkflowDocument({ children }: WorkflowDocumentProps) {
  return (
    <div className="px-6 py-6 space-y-8 max-w-[720px]">
      {children}
    </div>
  );
}

SECTION 4 — SINGLE REUSABLE DocSection COMPONENT
Create: client/src/components/docs/DocSection.tsx

This replaces ALL PRD cards.

PROPS CONTRACT (EXACT):

interface DocSectionProps {
  title: string;
  content: string;
  sectionKey: string;
  manuallyEdited?: boolean;
  onSave: (sectionKey: string, value: string) => void;
  onResetToAI?: (sectionKey: string) => void;
}

BEHAVIOR REQUIREMENTS
READ MODE:
- Plain text appearance
- Markdown rendered as prose
- No borders or inputs
- Cursor shows text caret

EDIT MODE:
- Click anywhere in content to edit
- Inline textarea replaces text
- Blur or Cmd+Enter saves
- Esc cancels edit
- No modal, no layout shift

CONTROLS:
- Edit icon appears on hover only
- Reset to AI shown only if manuallyEdited === true

SKELETON IMPLEMENTATION:

(import omitted for brevity in this instruction file)

SECTION 5 — SpecsTab INTEGRATION
Replace PRD rendering with DocSection usage.
Use existing saveWorkflowPRD logic.
Do not alter storage.

SECTION 6 — STYLING RULES
ALLOWED:
- Typography
- Spacing
- Hover affordances

FORBIDDEN:
- Cards
- Borders
- Shadows
- Background boxes
- Form-like visuals

ACCEPTANCE CRITERIA
- PRD reads like a document
- Inline editing works
- No visible inputs until interaction
- No cards or boxes
- AI regenerate still works
