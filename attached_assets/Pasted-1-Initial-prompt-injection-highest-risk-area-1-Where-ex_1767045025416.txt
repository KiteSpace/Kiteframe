1. Initial prompt injection (highest risk area)
	1.	Where exactly is initialPrompt injected into the chat flow?
	•	File + function name.
	•	Is it passed into handleSend as messageOverride?
	2.	Is initialPrompt consumed only once?
	•	After it is sent, is it cleared so it cannot re-trigger generation on re-render?
	3.	Does initialPrompt appear in chat history exactly as a normal user message would?
	•	Same role (user)?
	•	Same formatting?
	•	Same intent classification path?

⸻

2. “New workflow tab” semantics
	4.	What does “a new workflow tab is created” mean concretely?
	•	New project?
	•	New conversation instance?
	•	Or just opening the KiteAI Chat panel?
	5.	Is there any workflow or chat state that persists across Home Prompt submissions unintentionally?
	•	Especially currentWorkflowDraft or conversation history.

⸻

3. currentWorkflowDraft replacement semantics (must be explicit)
	6.	Can you confirm that edge/fail case expansion fully replaces currentWorkflowDraft, not merges with it?
	•	Please point to the line where replacement happens.
	7.	Is there any code path where expansion succeeds but currentWorkflowDraft remains unchanged?
	•	This must be NO.

⸻

4. Quick actions + Create Workflow binding
	8.	Are Preview / Create Workflow buttons rendered solely based on currentWorkflowDraft !== null?
	•	Please confirm there is no dependency on:
	•	message index
	•	message ID
	•	“first response” flags
	9.	After edge/fail case expansion, does Create Workflow always apply the expanded version?
	•	Even if the expansion was triggered by a quick action several turns later?

⸻

5. Failure and retry behavior (new logic mentioned)
	10.	When an AI call fails during expansion, what is the user-visible result?
	•	Does the previous draft remain intact?
	•	Are buttons still usable?
	11.	Does .finally() ever clear currentWorkflowDraft unintentionally?
	•	This should not happen.

⸻

6. Parity confirmation (final lock)
	12.	Have you manually verified the exact same prompt produces identical behavior when entered via:
	•	Home Prompt
	•	Typing directly into KiteAI Chat
	13.	Is there any remaining conditional logic that checks whether a prompt originated from Home Prompt?
	•	Ideally, this answer is NO.

⸻

7. One-sentence invariant check
	14.	Can you confirm this statement is true:

“Home Prompt is now a pure entry point; all workflow generation, expansion, preview, and creation logic lives exclusively in KiteAI Chat and shared components it owns.”
