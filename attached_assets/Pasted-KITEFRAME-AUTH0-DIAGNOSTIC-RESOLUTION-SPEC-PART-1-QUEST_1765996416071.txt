KITEFRAME — AUTH0 DIAGNOSTIC & RESOLUTION SPEC

PART 1 — QUESTIONS FOR REPLIT (MANDATORY DIAGNOSTICS)

A. CURRENT AUTH FLOW (AS IMPLEMENTED)
	1.	What is the exact Auth0 flow currently implemented?
	•	Universal Login (redirect)?
	•	Embedded login?
	•	Passwordless?
	•	Social-only?
	2.	Which SDK is used?
	•	@auth0/auth0-spa-js
	•	@auth0/nextjs-auth0
	•	Custom OAuth implementation?
	3.	Where does the decision logic live?
	•	Client-only
	•	Server-only
	•	Mixed (client starts flow, server validates session)?
	4.	What happens on these exact paths?
	•	New user → clicks “Sign in”
	•	Existing user → clicks “Sign up”
	•	User signs in with Google but account exists via GitHub
	•	User signs in via Replit → Google → Auth0

⸻

B. USER IDENTITY & ACCOUNT MATCHING
	5.	What is the canonical user identifier?
	•	Auth0 user_id?
	•	Email?
	•	Provider + provider_id?
	6.	Is account linking enabled in Auth0?
	•	Automatic?
	•	Manual?
	•	Disabled?
	7.	Do we allow multiple providers per user?
	•	Google + GitHub?
	•	Replit + Google?
	8.	If a user authenticates via a different provider but same email, what happens?
	•	New account created?
	•	Existing account reused?
	•	Login fails?

⸻

C. BETA & WAITLIST GATING
	9.	Where is beta eligibility checked?
	•	Before redirect to Auth0?
	•	After Auth0 callback?
	•	On every authenticated request?
	10.	What data determines beta access?

	•	Database flag (is_beta = true)?
	•	Email allowlist?
	•	Manual admin approval?

	11.	What happens when:

	•	User authenticates successfully but is not beta-approved?
	•	User is already waitlisted but tries again?
	•	User is beta-approved but signs in with a new provider?

⸻

D. SESSION & COOKIE STATE
	12.	How are sessions stored?

	•	HttpOnly cookies?
	•	JWT in localStorage?
	•	Auth0 session + app session?

	13.	What cookie settings are used?

	•	SameSite
	•	Secure
	•	Domain

	14.	Is there a mismatch between frontend and backend domains?

	•	*.replit.dev
	•	Custom domain?
	•	Auth0 callback mismatch?

⸻

E. FAILURE SIGNALS
	15.	Exact errors observed (copy logs):

	•	Auth0 callback errors
	•	Silent refresh failures
	•	401 after successful login
	•	Infinite redirect loops

	16.	Is the failure deterministic or provider-specific?

	•	Google only?
	•	GitHub only?
	•	Replit only?
