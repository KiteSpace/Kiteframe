SECTION 1 — SOURCE OF TRUTH (MOST IMPORTANT)

After the initial workflow is generated, where is the workflow state stored?

Is it stored in React state?

A conversation object?

A project draft?

Or only implicitly via the last AI message?

When the user clicks “Include edge / fail cases,” does the system:

Modify the existing workflow state?

Or generate a new workflow that is not persisted anywhere?

Which object is considered authoritative for “Create Workflow”?

The first generated workflow?

The most recent AI-generated workflow?

Or a separate draft object?

Is there a single “currentWorkflowDraft” concept shared across turns?

If not, why?

This section should reveal why “Apply/Create Workflow” keeps applying the original graph.

SECTION 2 — APPLY / PREVIEW BUTTON BEHAVIOR

How are “Preview” and “Create Workflow” buttons rendered?

Are they tied to the first AI message only?

Or re-rendered whenever a workflow changes?

When a follow-up AI response expands the workflow, why are Preview/Create not reattached?

Is the system assuming follow-ups are “discussion only”?

Is there a flag that indicates “this response contains a workflow”?

Is there a check like if (isInitialResponse) gating the buttons?

If so, why does it exist?

What condition currently enables Create Workflow?

Presence of nodes/edges?

A specific message type?

A confidence threshold?

This will surface that buttons are incorrectly coupled to message position, not workflow state.

SECTION 3 — EDGE / FAIL CASE EXPANSION FLOW

When the user selects “Include edge / fail cases,” what exact function is called?

Is it a different AI endpoint?

Or the same generator with a different prompt?

Does the expansion call receive the existing workflow as input?

Nodes + edges?

Or only the original user prompt?

Is the expanded workflow normalized and stored the same way as the initial one?

If not, where does it diverge?

After expansion, is analyzeWorkflowDiagnostics run again?

Or are diagnostics only computed once?

This determines whether expansion is treated as a mutation or just chat output.

SECTION 4 — HOME PROMPT VS KITEAI CHAT PARITY

Which path currently supports quick actions:

KiteAI Chat only?

Home Prompt only?

Or both, via shared logic?

Why was the quick-action logic not applied to Home Prompt?

Was it intentional?

Or was Home Prompt considered “one-shot”?

Do Home Prompt and KiteAI Chat share the same message rendering component?

If not, why are they diverged?

Is there a shared abstraction for “AI response with workflow + actions”?

Or are these handled independently per feature?

This will expose architectural duplication that needs consolidation.

SECTION 5 — CHAT STATE & TURN HANDLING

Does each AI message have a type (e.g. workflow, discussion, expansion)?

Or are all messages treated the same?

Is there a concept of “this message updates the workflow draft”?

If yes, how is that flagged?

If no, how does the system know when to update state?

Does the system ever overwrite the previous workflow draft?

Or only append chat messages?

Is conversation history used to infer state, or is state explicit?

If state is implicit, this explains the inconsistency you’re seeing.

SECTION 6 — WHY THIS BROKE (FORCING SELF-DIAGNOSIS)

Was the original design intent that only the first AI response could create a workflow?

If yes, that contradicts the new “expand workflow” behavior.

What would currently need to change so that “Include edge / fail cases” results in a new workflow draft that can be previewed and created?

List the concrete steps.

Is there any technical reason why expanded workflows cannot replace the original draft?

If yes, what is it?

If no, why wasn’t this implemented?

SECTION 7 — REQUIRED ALIGNMENT DECISION (FORCING COMMITMENT)

Can we agree that the following should be true?

At any time, there is exactly ONE active workflow draft

Any AI action that generates or expands a workflow replaces that draft

Preview/Create Workflow always operate on the current draft

If not, what alternative model is being used, and why?

This question forces them to either align or justify divergence.