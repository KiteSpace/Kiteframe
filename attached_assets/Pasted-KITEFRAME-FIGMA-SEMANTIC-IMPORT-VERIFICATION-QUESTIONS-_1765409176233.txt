KITEFRAME — FIGMA SEMANTIC IMPORT VERIFICATION QUESTIONS
========================================================

Please answer ALL questions below with either:
- direct code excerpts,
- file paths,
- or clear yes/no responses.

Do NOT summarize or approximate behavior. Provide exact implementation details.

========================================================
1. FILE STRUCTURE & PATHING
========================================================

1.1 Did you place the semantic files in the required locations?

- client/src/lib/integration/figmaSemanticTypes.ts
- client/src/lib/integration/figmaSemanticExtractor.ts

Answer:
Provide the exact file paths that now contain these modules.

1.2 Did any other file locations change?  
If yes, list them.

========================================================
2. TYPESCRIPT UPDATES
========================================================

2.1 Did you extend ImageNodeData to include:

figmaSemantic?: FigmaSemanticMetadata | null

Provide the exact updated interface from client/src/types.ts.

2.2 Did you update any other type definitions?  
If yes, list the changes.

========================================================
3. API BATCHING IMPLEMENTATION
========================================================

3.1 Did you implement a single request for multiple frame nodes using:

files/:fileKey/nodes?ids=id1,id2,id3,…

Provide the exact function signature and code for:

fetchFigmaFrameTrees()

3.2 Does convertFigmaToNodes.ts now rely on the batched response instead of one request per frame?

Provide the code snippet showing where it uses the batched nodesJson.

========================================================
4. SEMANTIC EXTRACTION LIMITS
========================================================

4.1 Did you implement MAX_DEPTH = 10?  
Show the exact line.

4.2 Did you implement MAX_ELEMENTS = 500?  
Show the exact line.

4.3 What is the behavior when limits are reached?  
Provide the relevant code.

========================================================
5. IMPROVED HEURISTICS
========================================================

5.1 Did you add BUTTON detection that checks:
- COMPONENT / INSTANCE role,
- TEXT child content,
- rectangle shape + text,
- corner radius or fill hints?

Provide the full heuristic functions:
- isButtonLike()
- isInputLike()
- looksLikeHeading()
- looksLikeSection()

5.2 Is the detection now language-agnostic (not relying solely on English words)?  
Explain how.

========================================================
6. ERROR HANDLING & FALLBACKS
========================================================

6.1 If semantic extraction fails, does the import:
- succeed,
- create the image node,
- set figmaSemantic = null?

Provide the code that performs this fallback.

6.2 Did you ensure no alerts or UI errors surface to the user?  
Confirm yes/no.

========================================================
7. TOKEN ROUTING (OAuth vs PAT)
========================================================

7.1 When calling fetchFigmaFrameTrees(), does pat default to undefined and rely on OAuth tokens when PAT is not used?

Provide the exact calling code.

7.2 Does /api/figma/proxy still correctly infer OAuth tokens stored in session?

Answer yes/no — no code needed unless modified.

========================================================
8. IMAGE NODE CREATION (NO CHANGES)
========================================================

8.1 Confirm that no existing properties in the image node were removed or modified.

Answer yes/no.

8.2 Provide the final node creation block showing:
- data.src
- data.originalWidth
- data.originalHeight
- data.figmaSemantic

========================================================
9. CONTEXT MENU (OPTIONAL)
========================================================

9.1 If you added a “View Semantic Data (debug)” entry, provide the implementation.

If not implemented, answer: NOT IMPLEMENTED.

========================================================
10. FUNCTIONAL TESTS
========================================================

10.1 Test case: Simple login screen.
Provide console output of node.data.figmaSemantic showing:
- heading
- input(s)
- button(s)
- form candidate
- navigation targets (if any)

10.2 Test case: Large frame.
Provide:
- figmaSemantic.elements.length
- confirmation that it is <= MAX_ELEMENTS

10.3 Test case: OAuth-only import.
Confirm imports succeed without providing a PAT.

========================================================
11. CONFIRM NON-GOALS ARE NOT IMPLEMENTED
========================================================

11.1 Confirm you did NOT:
- Generate workflow nodes from semantics
- Modify layout based on semantics
- Rebuild Figma components visually
- Sync back to Figma
- Create interactions/edges automatically

Answer yes/no to each.

========================================================
END OF VERIFICATION QUESTIONS
========================================================