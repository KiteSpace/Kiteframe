KITEFRAME — AUTH / LOGIN FLOW DEBUGGING INSTRUCTIONS
PURPOSE: Identify and fix incorrect client/secret + redirect configuration causing login failures.

====================================================
SECTION 1 — AUTH ARCHITECTURE CONFIRMATION
====================================================

1. Which OAuth providers are currently enabled?
   - [ ] Google
   - [ ] GitHub
   - [ ] Replit

2. Which auth framework is used?
   - [ ] Passport.js
   - [ ] NextAuth
   - [ ] Custom OAuth implementation
   - [ ] Mixed (explain why)

3. Is there a single unified auth pipeline for all providers?
   - YES / NO
   - If NO, list separate flows and entry points.

4. Are all providers funneled through a single callback handler?
   - YES / NO
   - If NO, list callback routes per provider.

====================================================
SECTION 2 — CLIENT ID / CLIENT SECRET CONFIGURATION
====================================================

5. Where are client IDs and secrets stored?
   - [ ] Environment variables
   - [ ] Replit Secrets panel
   - [ ] .env file
   - [ ] Hardcoded (THIS IS A BUG)

6. List the EXACT environment variable names in use:

   Google:
   - GOOGLE_CLIENT_ID = ?
   - GOOGLE_CLIENT_SECRET = ?

   GitHub:
   - GITHUB_CLIENT_ID = ?
   - GITHUB_CLIENT_SECRET = ?

   Replit:
   - REPLIT_CLIENT_ID = ?
   - REPLIT_CLIENT_SECRET = ?

7. Runtime verification:
   Confirm that these exist at runtime (do NOT log secrets).
   Example:
   console.log('[AUTH]', {
     googleClientIdExists: !!process.env.GOOGLE_CLIENT_ID,
     githubClientIdExists: !!process.env.GITHUB_CLIENT_ID
   });

   Provide confirmation logs.

====================================================
SECTION 3 — REDIRECT URI VALIDATION (CRITICAL)
====================================================

8. For EACH provider, list:
   - Redirect URI configured in provider dashboard
   - Redirect URI used in code

   Example:
   Provider Dashboard:
   https://kiteframe.app/auth/google/callback

   Code:
   https://kiteframe.app/api/auth/google/callback

9. Are there environment-specific redirects?
   - [ ] Local
   - [ ] Replit Dev
   - [ ] Production

10. Are ALL of these registered in the provider dashboards?
    - YES / NO

11. Check for mismatches:
    - http vs https
    - www vs non-www
    - trailing slashes
    - /auth vs /api/auth

====================================================
SECTION 4 — CALLBACK HANDLING
====================================================

12. Callback route file path(s):
    - Exact file location(s)

13. Is the callback route server-only?
    - YES / NO

14. Confirm callback is hit:
    - Add log at top of callback
    - Provide log output

15. Failure behavior:
    When login fails, what happens?
    - [ ] Redirect loop
    - [ ] Blank page
    - [ ] 401 / 403
    - [ ] OAuth error page
    - [ ] Silent failure

16. Provide exact OAuth error message note:
    (e.g. invalid_client, redirect_uri_mismatch, unauthorized_client)

====================================================
SECTION 5 — SESSION & COOKIE CONFIGURATION
====================================================

17. Session strategy:
    - [ ] Cookie-based
    - [ ] JWT
    - [ ] Server session store

18. Cookie settings:
    - Domain:
    - SameSite value:
    - Secure flag (true/false):

19. Are cookies blocked in:
    - iframe context?
    - cross-site redirects?
    - Replit environment?

20. Auth persistence:
    - Does user stay logged in on refresh?
    - Does auth break after routing to /home or /editor?

====================================================
SECTION 6 — BETA GATING INTERFERENCE
====================================================

21. When does beta gating run?
    - [ ] Before OAuth
    - [ ] After OAuth

22. Is the user being redirected away AFTER successful OAuth because they are not whitelisted?
    - YES / NO

23. Confirm separation of concerns:
    - Authentication (OAuth identity)
    - Authorization (beta access)

    These MUST be separate.

====================================================
SECTION 7 — REQUIRED OUTPUT FOR DIAGNOSIS
====================================================

Please provide:
1. List of all auth-related files (paths only)
2. One complete failing auth flow (provider → redirect → callback → failure)
3. Exact OAuth error message
4. Redirect URI from provider dashboard
5. Current environment (Replit / prod / local)

====================================================
LIKELY ROOT CAUSES (BASED ON PATTERNS)
====================================================

- Redirect URI mismatch
- Wrong client ID/secret loaded in Replit
- Cookie SameSite / Secure misconfiguration
- Beta gate blocking post-auth redirect
- Multiple auth pipelines conflicting

Once the above info is provided, we can:
- Pinpoint the exact bug
- Provide exact code changes
- Correct auth configuration
- Propose a clean, future-proof auth architecture