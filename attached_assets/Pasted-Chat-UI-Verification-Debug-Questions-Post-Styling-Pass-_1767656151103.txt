Chat UI Verification & Debug Questions (Post-Styling Pass)

A. User Message Bubble Styling (Grey Bubble Issue)
	1.	Where is user message styling defined now?
	•	Is it still inline conditional JSX in KiteAIChat.tsx, or has it been moved into ChatBubble.tsx?
	•	Please point to the exact file and classNames responsible for user message background color.
	2.	Is there a role-based style branch for role === 'user'?
	•	If yes, what classes are applied?
	•	If no, was user styling unintentionally flattened during refactor?
	3.	Is the grey background conditional on chat mode (panel vs fullscreen)?
	•	Could the user bubble styling be applied in one mode but not the other?
	4.	Are Tailwind utility classes being overridden?
	•	Is there any parent container applying bg-transparent, bg-background, or prose styles that could be overriding the bubble background?
	5.	Is the user message wrapped in a container that collapses background?
	•	For example:
	•	display: inline
	•	overflow-hidden
	•	prose styles applied incorrectly to user messages
	6.	Can you confirm whether consecutive user messages are grouped?
	•	If grouping logic was added, could it be skipping the bubble wrapper for grouped messages?

⸻

B. Assistant Message Container (Paragraph-Style Check)
	7.	Is the assistant message intentionally rendered without a bubble/container?
	•	If yes, confirm:
	•	No background
	•	No border
	•	Full-width prose layout
	•	If not, where is the container still being applied?
	8.	Is prose styling applied only to assistant messages?
	•	Confirm whether prose is scoped to assistant role only
	•	Ensure it is not leaking into user messages (which would suppress background colors)

⸻

C. Raw json Appearing in Assistant Responses ❗️(Critical)
	9.	Where does the json text come from when it appears?
	•	Is it:
	•	The literal string "json"?
	•	A fenced block like ```json?
	•	Raw serialized JSON object?
	10.	Is the AI response being passed through a JSON extraction utility?
	•	Which function is responsible?
	•	extractJSON()
	•	Regex-based parsing?
	•	Direct response.content rendering?
	11.	Are we rendering the raw model response instead of the cleaned output?
	•	Confirm whether the UI renders:
	•	parsed.content
	•	OR rawText
	•	OR a fallback when parsing fails
	12.	What happens when JSON parsing fails?
	•	Is there a fallback path that:
	•	Displays the raw response text?
	•	Prepends or leaves behind json markers?
	13.	Is the markdown renderer configured to strip code fences?
	•	Does react-markdown receive:
	•	Clean markdown?
	•	Or content that still includes ```json fences?
	14.	Do workflow-generation responses and chat responses share the same parsing path?
	•	If yes, could schema-driven parsing (for workflows) be leaking into chat rendering?

⸻

D. Rendering Pipeline Verification
	15.	What is the final render input to the chat UI?
	•	Before rendering, can you log:
	•	message.content
	•	message.role
	•	message.type
	•	Confirm that content is plain markdown, not serialized JSON.
	16.	Are tool/system messages being rendered as assistant messages?
	•	Could tool output (JSON) be mistakenly classified as assistant role?
	17.	Is there a “debug” or “development” flag causing raw output to show?
	•	Any conditional like if (debug) or showRawOutput?

⸻

E. Acceptance Criteria Check (Yes / No)

Ask Replit to answer explicitly:
	18.	Does the current UI meet these conditions?

	•	User messages have a visible grey bubble ✅ / ❌
	•	Assistant messages are paragraph-style with no bubble ✅ / ❌
	•	No avatars are shown ✅ / ❌
	•	No raw JSON or json markers ever appear in chat output ✅ / ❌
	•	Same styling applies in fullscreen and panel modes ✅ / ❌

If any answer is ❌, ask:
	•	Where exactly is the divergence introduced?
