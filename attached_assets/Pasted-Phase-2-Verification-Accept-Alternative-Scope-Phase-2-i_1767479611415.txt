Phase 2 Verification — Accept + Alternative

Scope:
Phase 2 introduces:

Commitment via Accept + undo

Comparison via Proposed vs Alternative toggle

All Phase 1 constraints remain in force.

Please answer each question with how it is implemented, not just yes/no.

1. Dual Variant Generation

Are Proposed and Alternative workflows generated:

In a single AI call?

From the same workflow snapshot?

Where are both variants stored?

Data structure

Lifetime of the cache

What prevents:

Regeneration on tab switch?

Regeneration on re-render?

2. Variant Toggle Behavior

How does the Proposed / Alternative toggle work?

Which state variable controls it?

What re-renders when it changes?

Confirm:

Toggle swaps preview instantly

Toggle does not mutate canvas state

Toggle does not trigger AI calls

3. Preview Correctness (Variant-Aware)

When viewing each variant, which nodes are rendered?

Origin nodes

Variant-specific additions

Confirm explicitly:

Only one variant is shown at a time

Full workflow is never rendered

Unrelated nodes never appear

Where is this logic enforced in code?

4. Accept Semantics (Critical)

What exactly happens when Accept is clicked?

Step-by-step

Which variant is committed?

How is the active variant determined at click time?

What metadata is attached to committed nodes?

Confirm:

Existing nodes are never modified or replaced

Only proposed additions are inserted

5. Undo / Redo Integrity

How many undo entries are created on Accept?

What does Undo remove?

What does Redo restore?

Confirm:

Cancel still creates zero undo entries

6. State Cleanup

What state is cleared after Accept?

What state is cleared after Cancel?

Confirm:

No proposal state persists after either action

A new proposal can be created cleanly

7. Regression Check (Phase 1 Guarantees)

Confirm none of the following changed in Phase 2:

Insight-scoped proposals

Surgical preview (origin + additions only)

Read-only preview canvas

Container-chrome-only distinction

Deterministic previews

If anything changed, explain why.

8. Known Weak Spots

Answer briefly:

What part of Phase 2 are you least confident in?

What would you refactor before Phase 3 if given time?

Phase 2 Acceptance Criteria

We will proceed to Phase 3 only if:

Comparison is deterministic and cached

Accept commits exactly one variant

Undo/redo works as a single atomic operation

No Phase 1 constraints were violated

Note

Phase 3 will introduce Experiments and pressure testing.
Any ambiguity in Phase 2 will compound in Phase 3.