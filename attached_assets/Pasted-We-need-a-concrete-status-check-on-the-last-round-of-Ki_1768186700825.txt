We need a concrete status check on the last round of KiteAI work. Please answer each question explicitly, with file names, functions, and confirmation of runtime behavior (not intent).

SECTION 1 — What was completed vs planned

1) Which of the following items were fully implemented in the last round?
   For each, answer: DONE / PARTIAL / NOT DONE, and list files touched.

   a) REPLACE workflow path that bypasses all merge/repair logic
   b) Removal of legacy REPLACE paths that routed through onApplyWorkflow
   c) Structural regression guard (branch depth / decision loss warning)
   d) Prevention of disconnected islands for CREATE / EXTEND
   e) Removal of KEEP option from UI and logic
   f) Confirmation dialog routing Replace → onReplaceWorkflow only
   g) Dev-only logging for Replace vs Apply confirmation
   h) Tests covering REPLACE behavior (unit or integration)

2) For any item marked PARTIAL or DONE:
   - What exact function enforces it?
   - Where is the guard located?
   - What would cause it to fail silently?

---

SECTION 2 — Edge-case expansion behavior (critical)

3) After the *first* edge/fail case expansion:
   - Is there any code that limits or changes behavior on subsequent expansions?
   - If yes, where is that logic implemented?
   - If no, confirm that repeated expansions are currently allowed to mutate the workflow.

4) When the AI is asked to “look for other missing failure cases”:
   - Is the system in DISCOVERY (non-mutating) mode or MUTATION mode?
   - What code determines that?
   - Why are ~10 existing nodes being modified instead of only adding new ones?

5) Is there any constraint that prevents:
   - Rewriting existing nodes
   - Rewiring unrelated edges
   - Introducing abstract catch-all nodes (e.g. “Error Handling”)
   If not, where should that constraint live?

---

SECTION 3 — Prompt vs execution alignment

6) Which prompt is used for:
   a) First edge/fail case expansion
   b) Subsequent “find missing cases” requests

   Please paste the exact prompt strings and explain how they differ in intent.

7) Do any prompts currently instruct the model to:
   - “Improve”, “clean up”, or “optimize” the workflow globally?
   - If yes, identify them — this may explain over-mutation.

---

SECTION 4 — Phase boundary enforcement

8) After the workflow reaches EXPANDED_WITH_EDGE_CASES:
   - Is there an explicit phase flag that changes AI behavior?
   - Or is behavior inferred only from conversation context?

9) Is there any guard that prevents:
   - Multiple mutating expansions in the same proposal lifecycle?
   - If not, confirm that this is currently unbounded.

---

SECTION 5 — Replace vs Create correctness

10) For REPLACE:
    - Confirm whether applyChatWorkflowMutation is ever called.
    - Provide logs proving it is NOT called.
    - If it is still called, identify why.

11) For CREATE / EXTEND:
    - What logic ensures new nodes attach to existing graph?
    - What happens when attachment confidence is low?
    - Why were workflows observed with lost depth or flattened branches?

---

SECTION 6 — Expected behavior confirmation

12) Please confirm whether the following statement is true or false:

   “After one successful edge-case expansion, further requests to identify missing failure cases should default to DISCUSSION ONLY and require explicit user selection before mutating the workflow.”

If false:
- Explain the current intended behavior.
- Explain how the system avoids runaway graph rewrites.

---

SECTION 7 — What needs to be fixed next (your recommendation)

13) Based on observed behavior and logs:
    - What do you believe is the single highest-risk gap remaining?
    - Which file should be fixed first?
    - What change would stop workflow degradation immediately?

Please answer in order, inline, with references to the codebase.