EXPERIMENT NODE — IMPLEMENTATION ALIGNMENT QUESTIONS (FOR REPLIT)

SECTION 1 — CURRENT GENERATION & LAYOUT BEHAVIOR
	1.	Where is the experiment branch layout logic implemented today?
	•	File(s) and function(s) responsible for positioning generated nodes
	•	Is layout direction hardcoded (left/right), inferred, or delegated to a layout plugin?
	2.	Can the current layout system detect nearby node density or free canvas space?
	•	Is there an existing utility to:
	•	Detect bounding boxes of nearby nodes
	•	Query empty space around a node
	•	If not, what layout context is currently available at generation time?
	3.	Are experiment branches treated as normal workflows by the layout engine?
	•	Or are they bypassing workflow-level layout logic and positioning nodes manually?

⸻

SECTION 2 — EXPERIMENT NODE STATE TRANSITION
	4.	Does the Experiment node currently convert into a standard node after generation?
	•	If yes:
	•	Where does that conversion happen?
	•	Is the node type changed, or just the data?
	•	If no:
	•	What prevents this today?
	5.	Is there any existing concept of “origin node” for a generated branch?
	•	How is the parent node tracked after generation?
	•	Is there a reliable way to reattach metadata (e.g. workflow header) to that origin?

⸻

SECTION 3 — WORKFLOW HEADER INTEGRATION
	6.	Can the existing WorkflowHeader component be attached dynamically to a subset of nodes?
	•	Is it strictly workflow-based today, or can it be scoped to a generated branch?
	7.	Is there existing support for header variants (color, actions, text)?
	•	If not, what is the cleanest extension point to add a purple “Experiment” variant?
	8.	How does the header currently determine which nodes it controls for drag-all?
	•	Does it rely on:
	•	Workflow IDs
	•	Node grouping
	•	Edge traversal
	•	Can this same mechanism be reused for experiment branches?

⸻

SECTION 4 — ACCEPT / REJECT / EDIT FLOW
	9.	Is there a clean way today to remove a generated branch without polluting undo history?
	•	If yes, where?
	•	If no, would this require a batched undo command?
	10.	Is there any existing mechanism to “re-open” a node into an editable experiment state?
	•	Or would Edit require:
	•	Reconstructing the Experiment node
	•	Restoring previous prompt / option selection
	•	Re-running AI from scratch?
	11.	Where should the experiment’s mode and prompt be persisted so Edit is possible?
	•	node.data?
	•	node.meta?
	•	separate experiment registry?

⸻

SECTION 5 — AI GENERATION PIPELINE
	12.	Where does experiment branch generation currently call KiteAI?
	•	Which function?
	•	What context is passed?
	13.	Can the AI generation pipeline be modified to return:
	•	Nodes
	•	Edges
	•	Recommended layout direction (horizontal vs vertical)?
	•	Or should layout always be determined client-side?
	14.	Is it feasible to tag generated nodes with meta.experimentId or similar?
	•	Needed to:
	•	Attach workflow headers
	•	Accept / reject cleanly
	•	Drag branches as a unit

⸻

SECTION 6 — EDGE CASES & CONSTRAINTS
	15.	What happens if the experiment origin node already has multiple outgoing edges?
	•	Can experiment branches coexist?
	•	Should we block generation in that case?
	16.	Are there any assumptions in validation, export, or PRD generation that all nodes are “committed”?
	•	If yes, where do we need to explicitly exclude experiment branches?

⸻

SECTION 7 — CONFIRMATION BEFORE BUILDING
	17.	Based on the above, which parts are already implemented vs missing?
	•	Please mark each as:
	•	✅ Exists
	•	⚠️ Partial
	•	❌ Missing
	18.	What refactors would be required before implementing this cleanly?
	•	Especially around:
	•	Layout
	•	WorkflowHeader attachment
	•	Undo/redo integrity

⸻

GOAL OF THESE QUESTIONS

These questions are meant to:
	•	Eliminate ambiguity before writing instructions
	•	Identify architectural blockers early
	•	Ensure the Experiment node behaves as a first-class workflow primitive, not a hack