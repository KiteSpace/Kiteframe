Critical Audit: Home Prompt → Chat Flow Misinterpretation

Context reminder: The Home Prompt does NOT open a modal. It expands into the same full-screen chat experience as KiteAI Chat. There is no modal UX in this flow.

SECTION 1 — WHAT DOES REPLIT THINK THE HOME PROMPT IS?

Which component do you believe the Home Prompt renders into after submission?

Please name the exact React component(s).

What UI surface do you believe appears after submitting the Home Prompt?

Modal

Drawer

Inline expansion

Route transition to chat screen

(If your answer includes “modal,” explain why.)

Where in the codebase did you introduce modal-related logic for the Home Prompt?

File name(s)

Component name(s)

Commit or diff reference

What existing code or comment led you to believe the Home Prompt should use a modal?

This will surface whether they inferred UX instead of reading code.

SECTION 2 — ACTUAL HOME PROMPT FLOW (FORCING CODE GROUNDING)

What route or state change occurs when the Home Prompt is submitted?

Is it a route push (e.g. /chat, /project/:id)?

Is it conditional rendering inside the same page?

Which component is responsible for rendering the full-screen chat UI?

Is this the same component used by KiteAI Chat?

Does Home Prompt reuse the same ChatContainer / MessageRenderer as KiteAI Chat?

If not, why?

If yes, where is the divergence?

At what exact point in the Home Prompt flow does your new modal code execute?

SECTION 3 — WHY A MODAL WAS EVEN POSSIBLE TO ADD

Was there already a modal component in the Home Prompt code path before your change?

Yes / No

If yes, what was it used for?

Did you introduce a new modal component instead of reusing existing chat UI?

If yes, what problem were you trying to solve?

Is the modal being used to represent currentWorkflowDraft state?

If so, why wasn’t that state rendered inline in chat?

This forces them to explain the architectural shortcut.

SECTION 4 — WORKFLOW DRAFT UI OWNERSHIP

Where is currentWorkflowDraft rendered in the Home Prompt flow right now?

Inline in chat?

In a modal?

In a separate overlay component?

Why is Create Workflow tied to a modal instead of the chat surface?

How does this modal interact with chat messages?

Does it block chat input?

Does it replace chat?

Does it float above it?

(Any of these answers should be “none,” which reveals the mistake.)

SECTION 5 — CHAT PARITY VIOLATION

Does KiteAI Chat use a modal for workflow creation?

If no, why does Home Prompt?

What is the explicit reason Home Prompt behavior diverges from KiteAI Chat behavior?

Is this intentional?

Or an implementation artifact?

What shared component should Home Prompt and KiteAI Chat both be using that was not reused here?

SECTION 6 — FAILURE MODE IDENTIFICATION (FORCING ADMISSION)

Did you treat the Home Prompt as a “one-shot generator” instead of a chat continuation?

If yes, that is incorrect — explain where that assumption came from.

Did you interpret “Create Workflow / Discard” as a confirmation dialog instead of a chat action?

If yes, why?

Were you optimizing for speed by adding a modal instead of correctly wiring chat state?

If yes, acknowledge this and explain what needs to change.

SECTION 7 — REQUIRED CORRECTION (LOCKING THEM IN)

Can you confirm the following is the correct model?

Home Prompt transitions into full-screen chat

Workflow generation, expansion, preview, and create all occur within chat

No modal is involved at any point

What code needs to be deleted or reverted to remove the modal behavior entirely?

List files and components.

What shared chat component should be used instead to render workflow draft actions inline?

SECTION 8 — SAFETY CHECK (PREVENTING REPEAT)

Before making further UI changes, will you verify behavior by navigating the app manually instead of inferring UX?

Yes / No

What concrete step will you take to ensure Home Prompt and KiteAI Chat cannot diverge again?

Shared component?

Shared state?

Shared renderer?