Phase 1 Review — Verification Questions

Scope: Propose Solution (Preview Only)

Before we proceed to Phase 2, please answer the following questions clearly and concretely.
For each answer, explain how it is implemented, not just whether it exists.

1. Proposal Scope & Insight Anchoring

How is a proposal scoped to a specific Insight?

What data is passed from the Insight item into proposal generation?

Is there an explicit insightId or equivalent identifier?

How are affected node IDs determined?

Are they computed automatically?

Passed from the Insight?

Hardcoded or inferred?

Confirm:

Proposals are not global

Proposals cannot modify unrelated parts of the workflow

Explain how this is enforced in code.

2. Preview Composition (Critical)

Exactly which nodes are rendered in the preview canvas?

Existing nodes: how are they selected?

Proposed nodes: how are they merged for preview only?

Confirm explicitly:

The full workflow is not rendered in the preview

Only origin node(s) + proposed additions are shown

Where does the preview graph data come from?

Cloned existing nodes?

New node objects?

Any shared references with the main canvas?

Please describe the data flow.

3. Canvas Isolation & Read-Only Guarantees

How is the preview canvas instantiated?

Separate KiteFrameCanvas instance?

Separate React tree?

How is readOnly={true} enforced?

At canvas level?

Node-level checks?

Both?

Confirm that in preview:

No node dragging is possible

No edge creation is possible

No keyboard shortcuts mutate state

Pan & zoom still work

If any interactions are selectively enabled/disabled, explain why.

4. State Management & Determinism

Where does proposal state live?

Local component state?

Global store?

Context provider?

Confirm:

Only one active proposal can exist at a time

Proposal state is fully cleared on Cancel

Proposal state is cleared on unmount / refresh

How is determinism ensured?

Is AI generation triggered only once per proposal?

Is there any chance of re-generation on re-render?

Please point to the guarding logic.

5. Cancel Semantics & Side-Effect Safety

What exactly happens on Cancel?

Which state variables are cleared?

Which components unmount?

Confirm:

No calls to withUndo

No calls to saveToHistory

No mutations to main canvas nodes or edges

How did you verify this?

6. Undo / History Hygiene

Confirm that Phase 1 creates zero undo entries.

Where in the codebase would an undo entry accidentally be created if a mistake were made?

What guards are in place to prevent that?

This is important to validate explicitly.

7. Layout & UI Structure

How is the two-column layout implemented?

Which layout primitives are used?

Are column widths fixed or resizable?

Confirm:

Left column = proposal description

Right column = preview canvas

Footer contains Cancel only

Is the preview container visually distinct only via chrome, not node styling?

Describe any deviations.

8. Feature Flag Enforcement

Where is the feature flag checked?

UI rendering?

Action handler?

Both?

Confirm:

The entire flow is inaccessible when the flag is off

No dead code paths trigger proposal generation accidentally

9. ID Safety & Edge Validity

How are IDs generated for proposed nodes and edges?

What prevents collisions with existing node IDs?

How is edge validity verified before rendering preview?

If validation exists, describe it.

10. Known Limitations & Assumptions

Please list explicitly:

Any assumptions you made that were not specified in Phase 1 instructions

Any shortcuts taken to meet scope

Any parts you consider fragile or temporary

This will inform Phase 2 planning.

11. Self-Assessment (Required)

Answer briefly:

What part of this implementation are you least confident about?

What would you refactor if Phase 2 were delayed?

Acceptance Criteria for Phase 1 Completion

We will move to Phase 2 only if:

Preview is correctly scoped (origin + additions only)

No global workflow replacement semantics exist

Cancel leaves zero residue

No undo history is polluted

Proposal logic is Insight-scoped, not header-scoped

Determinism is guaranteed