REPLIT — We need to diagnose why the multi-step Figma import is NOT working.

Below is the structured question set. Please answer every question explicitly and point to exact files/lines.

================================================================================
SECTION 1 — URL TYPE DETECTION (Frame vs Page vs File)
================================================================================

1. Is detectFigmaUrlType() implemented?
   - Where is it located? (file + line)
   - Does it return 'frame', 'page', or 'file' correctly?
   - Is it actually used inside FigmaImportModal.tsx?

2. When pasting a Figma link:
   - Does the modal detect the URL type?
   - Is ANY branching logic executed based on URL type? 
     (frame → direct import, page/file → user options)

3. Are we calling /files/{fileKey}/nodes?ids=... to check the node type?
   - If not, detection CANNOT work.

================================================================================
SECTION 2 — PAGE & FILE IMPORT FLOW
================================================================================

4. When a user inputs a PAGE or FILE URL:
   - Is the modal showing the expected Phase 1 screen with:
        • "Import page as single image"
        • "Select frames"
        • "Import frames from file"
     OR is the modal skipping directly to import?

5. Is FigmaImportModal.tsx still assuming “import everything immediately”?
   - Search for any code that directly calls onImport(...) without waiting for user choice.

6. Are we fetching the full Figma document tree?

   GET /files/{fileKey}

   If this call isn’t happening, the app CANNOT list frames.

7. Is frame enumeration implemented?

   - After calling /files/{fileKey}, do we iterate through:
        document.children[pageIndex].children.filter(node.type === 'FRAME') ?

8. Is the frame selection grid actually rendered?
   - If not rendered, why?
   - Is it unreachable due to skipped branching?

================================================================================
SECTION 3 — FRAME SELECTION GRID
================================================================================

9. Does the selection grid component exist?
   - File name?
   - Is it mounted in the modal?
   - What condition must be true for it to render?

10. Is selectedFrames[] being populated correctly?

11. When the user clicks "Continue":
   - Is onImport() called with the correct list of selected frames?
   - Or is it called with ALL frames?
   - Or is it never called?

================================================================================
SECTION 4 — NODE CREATION STAGE
================================================================================

12. When importing multiple frames:
   - Are nodes created for EACH selected frame?

13. Does each node include metadata:
      data.figmaSemantic
      data.isReferenceFrame
      data.figmaFileKey
      data.figmaNodeId

14. Are thumbnails generated per-frame OR is everything using a single page thumbnail?

15. Is semantic extraction running?

   - Check if extractFigmaSemanticMetadata(...) is called.
   - Log if figmaSemantic exists on created nodes.

================================================================================
SECTION 5 — WHY WORKFLOW GENERATION IS STILL MESSY
================================================================================

16. When clicking “Generate Workflow”:
   - Is the workflow generator using node.data.figmaSemantic?
   - Or is it falling back to a default generator that ignores metadata?

17. Is semanticWorkflowGenerator.ts even being called?

18. Is the generator receiving the node(s) the user expects?
   - Log all figmaSemantic objects passed into the generator.

19. Are edges being generated based on semantic workflowGraph?
   - Or are edges random / auto-created?

20. Is generator output being grouped properly into a *single workflow*?
   - Or is it creating 10–20 fragmented workflows?

================================================================================
SECTION 6 — UNINTENDED AUTO-GENERATION
================================================================================

21. Is workflow generation triggered ANYWHERE automatically during import?
   - It must ONLY run when user clicks “Generate Workflow”, never earlier.

22. Is the Layers panel incorrectly computing workflows from reference frames?
   - This could appear as new workflows even though no generator ran.

================================================================================
SECTION 7 — LOGS WE NEED FROM REPLIT
================================================================================

Please provide console logs for:

23. detectFigmaUrlType input + output

24. The response from GET /files/{fileKey}:
    - List all frames detected.
    - Log node types found under each page.

25. The frames passed into onImport()

26. The nodes created inside convertFigmaFrameToNodes()
    - Especially figmaSemantic

27. The input passed to semanticWorkflowGenerator()
    - How many nodes?
    - What semantic metadata?

28. The final workflow object returned by the generator.
    - Number of nodes
    - Number of edges
    - Grouping

================================================================================
SECTION 8 — EXPECTED BEHAVIOR WE NEED TO CONFIRM
================================================================================

29. For FRAME URL:
    - Should import ONE image node.
    - No workflow generated automatically.

30. For PAGE URL:
    - Should show OPTION C modal.
    - Should allow SELECT FRAMES.
    - Should ONLY import selected frames.

31. For FILE URL:
    - Should show pages + frames.
    - Should allow selecting frames across pages.

32. For workflow generator:
    - Should rely ONLY on figmaSemantic
    - Should NOT create orphaned nodes
    - Should NOT create chaotic workflows
    - Should NOT create workflow groups during import

================================================================================
END OF QUESTIONS
================================================================================
