REPLIT – FINAL REQUIRED QUESTIONS BEFORE IMPLEMENTATION
========================================================

SECTION A — Workflow Groups
1. Should workflow grouping run:
   a) On every change? (could be expensive)
   b) Only when user opens Project Panel?
   c) Only when PRD tab is opened?
   d) Only when user clicks “Generate Spec”?

2. Should workflow names be autogenerated (e.g., “Workflow 1”) or blank until user edits?

3. Should changes in workflow grouping (nodes splitting into separate workflows)
   trigger a PRD regeneration prompt?

--------------------------------------------------------

SECTION B — PRD Editing
4. Should manual PRD edits be considered “user-owned” content that AI cannot overwrite?
   - If yes, should AI regenerate only non-edited sections?

5. Should PRDs support rich text (markdown) editing inside the UI?

6. Should PRDs support versioning? (simple snapshot vs full history)

--------------------------------------------------------

SECTION C — Workflow Hash Behavior
7. Should we debounce hash generation (e.g., 500ms)?
8. Should hash be stored:
   - Per workflowId or per root node ID?
   - Should we persist workflowId in project metadata?

--------------------------------------------------------

SECTION D — PRD UX Integration
9. What behavior should happen when a PRD becomes stale?
   Options:
   a) Passive indicator
   b) Modal prompt
   c) Inline banner in Specs tab

10. When user regenerates PRD:
    - Should we show a diff view?
    - Or simply replace editable sections?

--------------------------------------------------------

SECTION E — Project PRD Scope
11. Should project PRD include:
    - User personas?
    - High-level architecture?
    - Assumptions?
    - Open questions?

12. Should project PRD auto-update when workflows change?
    - Always?
    - Only when user clicks “Update Project PRD”?

--------------------------------------------------------

SECTION F — Sources Integration
13. Should deleting a Figma frame (image node) remove it from Sources?
14. Should Sources display thumbnails or only links?
15. Should users be able to rename source entries?

--------------------------------------------------------

SECTION G — AI Behavior
16. Should PRD generator output structured JSON or markdown?
17. Should AI be allowed to reorder sections?
18. Should PRDs be stored as:
    a) JSON object with section fields
    b) Single markdown blob
    c) Hybrid (recommended)

--------------------------------------------------------

SECTION H — Performance
19. Are there workflow size limits we need to enforce?
20. Should we lazy-load PRDs per workflow?
21. Should PRD generation run in a Web Worker?

========================================================
END OF QUESTIONS