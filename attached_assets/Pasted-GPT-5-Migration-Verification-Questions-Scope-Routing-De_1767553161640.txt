GPT-5 Migration Verification Questions

Scope: Routing, Determinism, Auditability, Safety

Purpose:
Verify that GPT-5 is correctly enforced for workflow reasoning while preserving determinism, auditability, and Phase 2–5 guarantees. Answers must include code locations and behavioral explanations, not just yes/no.

1. AI Router & Task-Type Enforcement (Blocking)

Where is the single entry point for all AI calls now?

File path

Function signature

What taskType values exist, and which features map to each?

Confirm explicitly:

No feature file calls aiClient.chat() directly

No feature file specifies a hardcoded model

How does the router enforce:

GPT-5 for workflow_reasoning

GPT-5 for workflow_experiments

GPT-4o for vision_ingestion

User-selected model for general_chat

2. Hardcoded Model Removal

Which files previously hardcoded model: 'gpt-4o'?

How were they refactored to use the router?

Confirm:

There are zero remaining hardcoded model strings outside the router config

How did you verify this?

3. Session-Level Model Lock (Critical)

Where is the session model lock created?

What data structure stores it?

When is it initialized?

Which flows are model-locked?

Propose Solution

Experiments

(PRD generation, if applicable)

What happens if a user changes model settings mid-session?

Is the change blocked?

Deferred?

Ignored for the active session?

Confirm:

All AI calls within a session use the locked model

Cached previews cannot mix models

4. Determinism & Regeneration Safety

What guarantees that:

Proposed + Alternative use the same model?

All experiments use the same model?

If regeneration occurs:

Does it reuse the same locked model?

Or re-read settings?

Are there any remaining paths where:

A second AI call could read from localStorage again?

5. Output Parsing & Validation (High Risk Area)

How is structured output parsed now?

Parser implementation

Supported formats (raw JSON, fenced blocks, etc.)

Where does schema validation occur?

Zod (or equivalent) schema location

What happens on parse or validation failure?

Retry count

Model used for retry

Error surfaced vs silent sanitization

Confirm:

No silent model switching during parsing failure

Invalid output never reaches the canvas

6. Retry & Fallback Behavior

Under what conditions does a retry occur?

Is retry always performed with:

The same model?

The same provider?

Under what conditions does fallback occur?

Which model is used?

How is this recorded?

Confirm metadata is set correctly:

usedFallback

fallbackModelUsed

elevated uncertaintyLevel

7. Phase 5 Audit Compatibility (Required)

Where is model identity recorded?

DecisionSnapshot fields

Node provenance metadata (if applicable)

Confirm that for every accepted Proposal or Experiment we can see:

providerUsed

modelUsed

routerTaskType

fallback status (if any)

Does this metadata:

Survive undo/redo?

Appear in audit exports?

8. Settings UX Scope Enforcement

What does the “Choose model” setting affect now?

Confirm explicitly:

It affects general chat only

It does NOT affect workflow reasoning

It does NOT affect experiments

If a user selects GPT-4 or GPT-3.5 in Settings:

What model is used for workflow generation?

9. Feature Flag Behavior

Where is ENABLE_GPT5_WORKFLOW_REASONING checked?

What changes when the flag is:

OFF

ON

Can the flag be toggled without redeploy?

Does disabling the flag:

Fully revert to pre-migration behavior?

Leave routing logic intact?

10. Vision Ingestion Safety

Which tasks still use GPT-4o?

How does the router detect vision usage?

Confirm:

Vision ingestion never routes to GPT-5 (unless explicitly enabled later)

Vision and workflow reasoning are never mixed in one call

11. Regression Check (Phases 2–5)

Confirm none of the following changed:

Insight-scoped proposals

Deterministic previews

Proposed vs Alternative comparison

Experiment behavior

Accept / Cancel semantics

Undo atomicity

Phase 4 heuristic application

Phase 5 explainability guarantees

If anything changed, explain why.

Final Question (Most Important)

If a workflow node is added today, can we unambiguously answer:

which model generated it

why that model was chosen

whether fallback occurred

and whether the user could have influenced that choice

If the answer is “yes,” explain how.
If “no,” explain what is missing.

Migration Acceptance Criteria

This migration is approved only if:

GPT-5 is enforced for workflow reasoning

User settings cannot downgrade it

Sessions are model-locked

Model identity is auditable

No hardcoded models remain

Vision import still works

No Phase 2–5 guarantees regress