REPLIT: I need the following 12 items returned as direct code excerpts, file paths, and implementations — NOT summaries.

Please quote the actual code from the repo for each answer.

---------------------------------------
1) FILE STRUCTURE (FULL)
---------------------------------------
Provide the exact directory tree for:
- Node renderers
- Node registry
- Canvas components
- Editor state management
- Plugin/hook system (if present)
- Any context providers (EditorContext, CanvasContext)

---------------------------------------
2) NODE INTERFACE (AS IMPLEMENTED)
---------------------------------------
Provide the exact current TypeScript interface or shape for Node as implemented in the codebase, including all fields.

---------------------------------------
3) WHERE NODES ARE RENDERED
---------------------------------------
Provide the component file and code snippet where nodes are mapped and rendered on the canvas (e.g., nodes.map(...)).

---------------------------------------
4) NODERENDERER API
---------------------------------------
Provide the exact props accepted by the NodeRenderer component, including its full function signature.

---------------------------------------
5) NODE FOOTER COMPONENT
---------------------------------------
Provide the full JSX and file path of the node footer area used by all nodes, where we will inject the status badge.

---------------------------------------
6) WORKFLOW STORAGE / GROUPING LOGIC
---------------------------------------
Provide the file(s) and code where:
- Workflow metadata is stored
- Workflow groups are determined
- Layers panel workflow groups are shaped or computed

---------------------------------------
7) READ-ONLY MODE IMPLEMENTATION
---------------------------------------
Provide the code that:
- Defines the read-only prop
- Passes read-only to nodes
- Disables editing/mutations when read-only

---------------------------------------
8) NODE MUTATION PIPELINE
---------------------------------------
Provide:
- The setNodes implementation
- Any helper functions used to update nodes
- Any reducers or mutation utilities
- Undo/redo logic if implemented

---------------------------------------
9) LAYERS PANEL WORKFLOW GROUPING
---------------------------------------
Provide the file and code that:
- Renders workflow groups in the layers panel
- Handles workflow renaming
- Lists nodeIds for workflow groups

---------------------------------------
10) EXISTING MCP / FIGMA CONNECTOR CODE
---------------------------------------
Provide code for any MCP integration, including:
- Tool registration
- Calls made to retrieve design metadata
- Any placeholder or partial Figma functions

---------------------------------------
11) PROMPT CONTAINER (HOMEPAGE)
---------------------------------------
Provide the file and JSX where the homepage “prompt” container exists, including the upload image button.

---------------------------------------
12) PROJECT CREATION FLOW
---------------------------------------
Provide the files and code that:
- Create a new project
- Initialize project metadata
- Handle routing/transition from homepage → project view
- Store imported artifacts (if any exist)

---------------------------------------

IMPORTANT:
Return actual code blocks for each item.
Do not summarize or paraphrase.