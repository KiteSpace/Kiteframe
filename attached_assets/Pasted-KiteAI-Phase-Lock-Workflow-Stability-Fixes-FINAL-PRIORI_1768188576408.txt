KiteAI – Phase Lock & Workflow Stability Fixes (FINAL)

PRIORITY 0 — Emergency Stop (Ship Immediately if Needed)

0.1 Disable mutation after first expansion
File: client/src/components/KiteAIChat.tsx
- After workflowGenState === 'EXPANDED':
  - Disable onAccept / Create Workflow action
  - Show toast: "Workflow expanded. Discuss further or apply selected changes."
- This prevents runaway mutations immediately.

--------------------------------------------------

PRIORITY 1 — Phase Lock (Correct Long-Term Fix)

1.1 Add explicit expansion lock state
File: KiteAIChat.tsx
- Add state: hasExpandedOnce: boolean
- Set to true after first successful edge-case expansion

1.2 Gate quick actions after expansion
File: WorkflowDraftActions.tsx
- When hasExpandedOnce === true:
  - Replace "Include Edge / Fail Cases" with "Discuss Missing Cases"
  - Remove auto-mutation paths
  - Require explicit user confirmation ("Apply These Changes") to re-enable mutation

1.3 Add execution guard (non-UI)
File: KiteAIChat.tsx (handleAcceptWorkflow)
- If hasExpandedOnce === true AND user has not explicitly confirmed mutation:
  - Block apply
  - Show warning toast
This ensures state desync cannot cause mutation.

--------------------------------------------------

PRIORITY 2 — REPLACE Safety Hardening

2.1 Runtime assertion for REPLACE mode
File: applyChatWorkflowMutation.ts
- Add assertion:
  - If mode === 'REPLACE' and any merge/repair function is invoked:
    → throw error in dev
    → block mutation in prod
This guarantees REPLACE never silently regresses.

2.2 Remove KEEP button completely
File: workflow-editor.tsx
- Remove "Keep Existing" UI
- Remove related handlers/toasts
- Dialog should only show:
  - "Replace (Undoable)"
  - Close (X)

--------------------------------------------------

PRIORITY 3 — Branch Overwrite Guard (Secondary but Important)

3.1 Prevent overwrite of branching nodes during merge
File: applyChatWorkflowMutation.ts
- Before merge:
  - Detect existing nodes with outboundEdges.length > 1
  - Block overwrite of those nodes
  - Allow additive children only
- This prevents accidental branch flattening in CREATE/EXTEND flows.

--------------------------------------------------

PRIORITY 4 — Tests (Blocking for Launch)

4.1 replace-workflow.test.ts
- REPLACE bypasses merge/repair
- Single undo restores previous workflow

4.2 structural-regression.test.ts
- Flattening proposal triggers warning modal

4.3 expansion-lock.test.ts
- After first expansion:
  - Mutation blocked
  - Discussion-only allowed
  - Explicit confirmation required to mutate again

--------------------------------------------------

PRIORITY 5 — Cleanup

5.1 Gate dev-only logs behind flag
5.2 Remove temporary console logs after verification