### SECTION 1 — PROJECT SCOPING FOR LAYERS PANEL FIX

1. Does the current Layers panel implementation (LayersPanel.tsx and related stores:
   - collapseStore
   - visibilityLockStore
   - workflowNameStore
   - nodeToWorkflowStore
   )
   persist state globally across ALL projects rather than per-project?

2. When switching between projects in the UI, are the following cleared or reinitialized?
   - collapse state
   - visibility state
   - lock state
   - workflow name overrides
   - computed workflow groups from FlowDetection

3. Confirm that LayersPanel receives projectId as a prop OR can access it via context/store.
   If not, where should this prop be injected?

4. Is there any existing store or abstraction we can use to namespace LayerPanel state per project
   (e.g., projectScopedStore(projectId)) or should we create a new system?


### SECTION 2 — FIGMA IMPORT + FRAME SELECTION

5. Currently, does FigmaImportModal show:
   (a) a grid of frames to pick from, OR
   (b) import everything automatically?

6. Please confirm how many frames are being inserted into the project canvas when the user completes import.
   Is it:
   - one reference frame image node?
   - one image node per selected frame?
   - OR imports all frames even without user selection?

7. After import, where is the frame selection state stored?
   - in React component state?
   - in a global store?
   - not stored at all?

8. Is there any existing logic that generates workflows during import?
   (We expect: NO — only nodes should be created, workflows should only be created *after* the user explicitly clicks Generate Workflow.)


### SECTION 3 — REFERENCE FRAMES

9. Are Figma-imported image nodes currently tagged with identifiable metadata?
   e.g., node.data.figmaId, node.data.figmaFileKey, node.data.figmaSemantic?

10. Do you think node.data.isReferenceFrame is the correct flag OR do you prefer a different naming convention?

11. Should reference frames automatically be added to the Sources tab, or should this be handled later?


### SECTION 4 — WORKFLOW GENERATOR

12. When `Generate Workflow` is clicked, which function currently runs?
    - It should be generateWorkflowFromFigmaImageNode(). Please confirm.

13. Confirm the Workflow Generator currently:
    - creates multiple floating nodes,
    - creates orphan workflows,
    - does NOT group outputs into a single workflow group.

14. Should the output of Generate Workflow:
    (a) always create exactly ONE workflow group with all generated nodes?
    (b) create one workflow per selected frame?
    (c) let user choose via a popover?

15. Is there an existing internal action for:
    - batch node creation with undo grouping
    - batch edge creation
    - layout utilities (horizontalFlow, verticalFlow, grid, etc.)

16. Confirm that the existing LayoutPlugin supports calling a layout AFTER nodes are inserted.


### SECTION 5 — INSERTION POSITIONING & MULTI-SELECT

17. Does createFigmaProject.ts or convertFigmaFrameToNodes.ts support “relative placement around root node”?
    If no, we will provide instructions for implementing root-relative placement.

18. Do we have access to:
    - getSelectedNodes()
    - setSelectedNodes()
   so we can automatically multi-select all generated nodes after creation?

19. Confirm that undoManager.batch() is available and stable for bundling all generated nodes/edges into a single undo step.


### SECTION 6 — PERFORMANCE & BATCHING

20. What is the maximum number of nodes you’ve seen generated from semantic extraction so far?

21. Can we safely assume the semantic structure is small (< 50 nodes) after the improvements?

22. Do you prefer:
    - a single consolidated AI call for all selected frames?
    - one AI call per frame?
   (We recommend single call.)


### SECTION 7 — OPTIONAL BUT IMPORTANT

23. When inserting multiple reference frames in a single import, should we:
    (a) collapse them into a single workflow?
    (b) display them as separate workflows?
    (c) never generate workflows automatically (preferred), and rely solely on the user clicking Generate Workflow?

24. Should the thumbnail image remain the only visual representation, or do you want us to add an optional “Render full-resolution preview” action later?

25. Is the expected behavior:
    - reference frames only inserted as image nodes,
    - user explicitly triggers workflow generation,
    - no workflows are created implicitly,
    correct?