KITEFRAME — REPLIT QUESTIONS
Fixing KiteAI “looping” + defining Fast Path vs Guided Mode

GOAL
We need to permanently stop the “ask again after user says Generate” loop and define Fast Path rules that override AI uncertainty. Please answer precisely and point to files/functions.

1) SOURCE OF TRUTH (State Machine)
- Does KiteAI have an explicit state machine (clarification/execution-ready/executing/done)?
  If yes: where is state stored and what code transitions backward (execution-ready -> clarification)?
  If no: what heuristic decides “ask questions” vs “generate now”?

2) EXECUTION-READY DEFINITION
- Where is “execution-ready” computed (exact function/file)?
- Is there a single authoritative gate, or multiple gates in different components?
- Can user intent override it today? If yes, where?

3) FAST PATH OVERRIDE (Hard Intent)
We want this rule: “UI actions + explicit user commands override confidence.”
Confirm whether these should trigger fast path (no further clarification):
- Clicking Generate (prompt chat)
- Clicking Generate in Experiment node
- User says “Generate”, “Create the workflow”, “Looks good”, “Make assumptions”
- Upload image/figma and user clicks Generate
If any cannot, explain the technical blocker.

4) LOOP PREVENTION
- Is there any loop detection today?
- If not: are you OK implementing:
  - MAX_CLARIFICATION_TURNS = 2
  - After that: either generate with assumptions OR ask 1 blocking question only
- Where should this cap be enforced (exact location)?

5) CONFIDENCE BEHAVIOR
- How is confidence calculated today (model output vs heuristic)?
- Can low confidence currently block generation even when user confirms?
  If yes: where is that enforcement and is it intentional?

6) WORKFLOW GENERATION CONTRACT
When the user confirms/clicks Generate:
- Should we always generate immediately (and allow edits later)?
- Are there any scenarios where we refuse to generate entirely? List explicit criteria.

7) EXPERIMENT NODE ALIGNMENT
Confirm desired behavior:
- Experiment Generate = always fast path (no clarification, no confidence gating)
- After Generate: preview branch only
- Accept/Reject controls finalization (no re-entry to guided mode)
If current code violates this, point to where.

8) CENTRALIZATION
Can we enforce all of the above in ONE gate function?
- If yes: propose the location (e.g., shouldExecuteFastPath(context))
- If no: list the specific files that must change.

END