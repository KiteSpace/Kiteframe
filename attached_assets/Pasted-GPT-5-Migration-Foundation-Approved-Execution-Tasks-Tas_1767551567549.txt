GPT-5 Migration Foundation — Approved Execution Tasks

Task 1 — Create Central AI Router (Blocking)
	•	Implement aiRouter.chat({ taskType, messages, sessionId?, metadata? })
	•	All AI calls MUST go through this router
	•	No feature may call aiClient.chat() directly after this task
	•	Router is responsible for:
	•	Model selection
	•	Provider selection
	•	Capability checks (vision vs text)

Acceptance:
	•	Grep confirms no direct aiClient.chat() usage remains

⸻

Task 2 — Define Task-Type Model Policy (Blocking)
Implement routing rules:
	•	workflow_reasoning → GPT-5 (system enforced)
	•	workflow_experiments → GPT-5 (system enforced)
	•	prd_generation → GPT-5 (system enforced)
	•	vision_ingestion → GPT-4o
	•	general_chat → user-selected model

Rules:
	•	User settings MUST NOT affect workflow reasoning
	•	Settings apply only to general_chat

Acceptance:
	•	Changing model in Settings affects chat only
	•	Workflow generation always routes to GPT-5 when enabled

⸻

Task 3 — Remove All Hardcoded Models (Blocking)
Refactor and remove all model: 'gpt-4o' from:
	•	prdEngine.ts
	•	semanticWorkflowGenerator.ts
	•	Any other feature files

Acceptance:
	•	No hardcoded model strings remain outside router config

⸻

Task 4 — Session-Level Model Lock (Critical)
	•	Lock { provider, model } at session start for:
	•	Propose Solution
	•	Experiment
	•	All calls within session must reuse the lock
	•	Settings changes mid-session apply only to future sessions

Acceptance:
	•	Mid-session model changes do not affect active proposal/experiment
	•	Cached previews remain consistent

⸻

Task 5 — Record Model Provenance (Phase 5 Required)
Extend DecisionSnapshot with:
	•	providerUsed
	•	modelUsed
	•	routerTaskType
	•	usedFallback
	•	fallbackModelUsed (if any)

Rules:
	•	Written once at Accept
	•	Immutable
	•	Survives undo/redo
	•	Included in audit export

Acceptance:
	•	Every committed node has traceable model provenance

⸻

Task 6 — Harden Structured Output Parsing (Critical)
	•	Replace regex JSON extraction
	•	Use tolerant parser + Zod schema validation
	•	Parsing rules must support:
	•	raw JSON
	•	fenced code blocks
	•	On failure:
	1.	Retry once with same model
	2.	If still invalid, return controlled error
	3.	Do NOT switch models silently

Acceptance:
	•	GPT-5 outputs parse reliably
	•	Malformed output never reaches canvas

⸻

Task 7 — Controlled Retry & Fallback Logic
	•	Automatic retry: max 1, same model
	•	Fallback:
	•	Only after retry
	•	GPT-5 → GPT-4o
	•	Must set:
	•	usedFallback = true
	•	fallbackModelUsed
	•	elevated uncertaintyLevel

Acceptance:
	•	Fallback events are visible in DecisionSnapshot
	•	No silent model switching

⸻

Task 8 — Feature Flag GPT-5 Enforcement
	•	Add ENABLE_GPT5_WORKFLOW_REASONING
	•	Flag gates routing logic (not UI)
	•	OFF → current behavior
	•	ON → GPT-5 enforced for workflow reasoning

Acceptance:
	•	Flag flip cleanly enables/disables GPT-5 without code changes

⸻

Task 9 — Update Settings UX Scope (Required)
	•	“Choose model” applies only to general chat
	•	Workflow reasoning ignores user choice
	•	Add copy if needed:
“Workflow generation uses a system-recommended model for reliability.”

Acceptance:
	•	Users cannot downgrade workflow reasoning model

⸻

Task 10 — Migration Verification Checklist
Confirm:
	•	GPT-5 is default for workflow generation
	•	Sessions are model-locked
	•	No hardcoded models remain
	•	Model identity is auditable
	•	Vision import still works
	•	General chat model selection still works
	•	Phase 2–5 guarantees are intact
