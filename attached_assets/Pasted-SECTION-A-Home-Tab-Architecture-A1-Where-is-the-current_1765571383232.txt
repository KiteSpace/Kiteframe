SECTION A — Home Tab Architecture
A1. Where is the current Home tab component located?

Common guesses:

src/pages/Home.tsx
src/components/Home/HomePage.tsx
src/components/dashboard/Home.tsx


We must know the exact file path.

A2. How is the Home page currently structured?

Specifically:

Is the hero input inside its own component?

Is the recent projects list a separate component?

We need the current JSX tree.

SECTION B — State Management
B1. Does Home use local state, Zustand, Redux, or something else?

We need to know where to place:

isPreProjectChatOpen
preProjectChatHistory[]

B2. Is there already a chat store (for the right-side panel AI inside projects)?

If yes:

Can we reuse it for pre-project chat?

Or must the pre-project chat be isolated?

SECTION C — API Integration
C1. How does the existing chat call the backend?

We need the exact pattern:

fetch(/api/chat)

custom streaming hook

Vercel AI SDK

Serverless function

Custom backend

C2. Are we allowed to create a new endpoint like /api/preproject/chat?

Or must pre-project chat reuse /api/chat?

SECTION D — Styling / Component Library
D1. Does the repo use:

Tailwind

CSS Modules

Styled Components

Chakra

MUI

Vanilla CSS

D2. Are gradients already defined somewhere, or is the morphing gradient new?
D3. Are global layout constraints (max width, responsive breakpoints) defined in a shared layout file?

We need this for the hero component.

SECTION E — Asset & Icon Management
E1. Where are icons imported from?

Lucide

Heroicons

Custom SVGs

We need to match existing patterns for:

Back button

Document icon

Figma icon

Attachment icon

SECTION F — Creating a Project After Chat
F1. What function currently creates a project?

We need its signature.

Example guesses:

createProject({ name, description, nodes, edges })
navigate(`/projects/${id}`)

F2. Can we generate a project without nodes?

In many cases the user will provide:

Only text → then AI must generate workflow nodes

A document → AI must generate workflow nodes

A figma file → triggers existing Figma import flow

We need to know which patterns are allowed.

SECTION G — Validation
G1. Does the Home page already accept file uploads anywhere?

If yes:

Which components handle them?

Do they exist in a reusable form?

If not:

Do we implement new upload components?