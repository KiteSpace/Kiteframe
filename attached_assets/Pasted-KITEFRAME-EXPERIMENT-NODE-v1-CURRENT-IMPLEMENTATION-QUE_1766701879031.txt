KITEFRAME — EXPERIMENT NODE v1 (CURRENT IMPLEMENTATION QUESTIONS FOR REPLIT)
Purpose: Validate the current architecture + identify deltas vs the intended UX so we can write correct “do-this-exactly” implementation instructions.

--------------------------------------------
0) QUICK CONTEXT RECAP (FOR REPLIT)
Goal UX:
- User selects mode + suggestion (or free-form prompt) → Generate
- Experiment node converts into a normal committed node (process) with selected text
- Generated branch is laid out intelligently (avoid overlap, choose horizontal/vertical based on space)
- A purple Experiment Workflow Header appears over the origin (scoped to this experiment branch)
  - Label: “What If Experiment” / “Risk Experiment” / “Enhancement Experiment”
  - Buttons: Accept / Reject
  - Dropdown: Edit (reopens experiment selector and allows changing selection)
  - Drag handle moves entire experiment branch together (existing behavior in WorkflowHeader)

--------------------------------------------
1) CURRENT SOURCE OF TRUTH (FILES + FUNCTIONS)
1.1 Confirm the current files and functions used for Experiment branch generation:
- generateWildCardBranch.ts (or renamed to generateExperimentBranch.ts?)
- parseGeneratedBranch() (layout logic)
- workflow-editor.tsx handlers:
  - onWildcardGenerateBranch / onExperimentGenerateBranch
  - onExperimentAdoptBranch
  - onExperimentDiscardBranch

1.2 Provide exact line references and a short explanation for:
- Where AI request is made (ai.chat call)
- Where JSON is parsed and validated
- Where nodes/edges are created and pushed into state
- Where generatedIds are stored

--------------------------------------------
2) AI GENERATION HOOKUP (NOT WIRED YET)
2.1 Is the Experiment “Generate” button calling the handler?
- Confirm onGenerateBranch prop is passed through KiteFrameCanvas → ExperimentNode → footer button
- Confirm click event triggers the handler every time (no silent failures)

2.2 What is the current API endpoint being called?
- /api/ai/chat ?
- Which payload keys are sent for Experiment generation?
- Is this using the same AiProvider selection logic as KiteAI chat?

2.3 What errors are happening when “Generate” is clicked?
- Please paste server logs + client logs when generation fails
- Confirm if failures are networking (HTTP2 ping fail / timeout) vs prompt/JSON parsing failures

2.4 Confirm the exact model used for Experiment generation today:
- OpenAI GPT-4o vs other model
- Does it respect the same “provider selected by user” setting?

DECISION TO LOCK (for v1):
- Use the user-selected AiProvider for BOTH option generation and branch generation.
- Provide confirmation that the current provider selection is accessible in the Experiment generation handler.

--------------------------------------------
3) OPTION GENERATION (PREDICTIVE SUGGESTIONS) — STATUS CHECK
3.1 Is option generation implemented at all?
- If yes: where are suggestions stored? (node.data.suggestions? node.meta.suggestions? local component state?)
- If no: confirm there is no API call happening on connect/mode-change yet.

3.2 Trigger timing (confirm what you implemented vs what we want):
- When the Experiment node first becomes connected to a workflow node (first incoming edge)
- When mode changes
- Manual “Refresh suggestions” button

3.3 Caching:
- Are suggestions cached per experimentNodeId + mode + originNodeId?
- Are you caching in-memory only, or persisted in node.data for reload/save?

--------------------------------------------
4) “GENERATE” WORKFLOW BEHAVIOR (INTENDED VS CURRENT)
4.1 Current behavior after Generate:
- Does it create nodes immediately, or does it wait for Adopt?
- Does it flag nodes as speculative/preview?
- Does it create generatedNodeIds + generatedEdgeIds?

4.2 Intended behavior (lock for v1):
- Generate creates the branch in a “reviewable” state with Experiment Workflow Header (Accept/Reject).
- Accept commits (single undo step)
- Reject removes (no undo noise if possible)

Confirm whether your current implementation:
- Removes on reject by filtering generated IDs
- Adds undo history on reject (currently yes) and why
- Can batch commands in UndoRedoManager to avoid noisy history

--------------------------------------------
5) LAYOUT: AVOID OVERLAP + PICK HORIZONTAL/VERTICAL
5.1 Current layout logic:
- Confirm parseGeneratedBranch currently places nodes right-to-left with baseX + index*spacing
- Confirm spacing is constant and not based on node count or density

5.2 Needed additions:
- Do we already have a utility to compute bounding boxes for nodes? (e.g., getNodesBounds / computeBounds)
- Do we have a “nearby nodes” query (within radius) or a spatial index?

5.3 Proposed v1 implementation question:
- Would you prefer to implement a small “SpaceProbe” utility that:
  - Takes origin position + list of existing node bounds
  - Samples candidate directions (right/left/up/down)
  - Picks the direction with least overlap risk
  - Applies dynamic spacing based on N generated nodes
Confirm feasibility and where you would locate this utility.

5.4 Collision handling:
- If the chosen direction still collides, what is your fallback?
  - Increase spacing
  - Switch axis
  - Offset diagonally
Confirm which fallback you will implement.

--------------------------------------------
6) EXPERIMENT NODE → NORMAL NODE CONVERSION (GRADUATION)
6.1 Current behavior:
- You said conversion happens only on Adopt.
Confirm:
- Node type changes wildcard/experiment → process
- data is simplified (mode/selection lost)
- Where is the conversion code?

6.2 Intended behavior (lock for v1):
- After Generate, the origin Experiment node should convert into a normal committed node immediately
  - It becomes the “origin committed node” with label/content = selected option or prompt
- We must preserve experiment metadata for Edit in node.meta.experiment
Questions:
- Can you persist experiment metadata in node.meta without breaking serialization/import/export?
- Confirm where node.meta is typed and persisted.

--------------------------------------------
7) WORKFLOW HEADER FOR EXPERIMENT BRANCH (PURPLE VARIANT)
7.1 Current capability:
- WorkflowHeader is currently attached to FlowDetection groups (connected components).
Confirm:
- How FlowDetection groups are computed today
- Whether generated experiment branch nodes form a separate flow group or merge into existing workflow

7.2 Needed behavior:
- We need a branch-scoped header even if the branch is part of an existing workflow group.
Questions:
- Are you willing to implement an “OverlayHeader” anchored by experimentId instead of flowId?
- Alternatively, can we create a derived “virtual flow group” for experiment branches based on meta.experimentId?

7.3 Drag-all:
- WorkflowHeader drag-all currently uses flowId grouping.
Question:
- Can we reuse the same drag-all logic by swapping “flow node set” with “experiment branch node set” (filtered by meta.experimentId)?

7.4 Header styling:
- Confirm where WorkflowHeader themes/variants are defined (themes.ts).
- Confirm you can add a purple “ExperimentHeader” variant without affecting normal headers.

--------------------------------------------
8) ACCEPT / REJECT / EDIT (LIFECYCLE + DATA RETENTION)
8.1 Reject:
- Should remove generated nodes/edges + remove branch header.
Question:
- Can we do this without creating a new undo step?
  - Option A: “silent” discard (no saveToHistory) for reject
  - Option B: one undo step labeled “Reject experiment branch”
Confirm which is realistic given the current history system.

8.2 Accept:
- Should keep nodes/edges and remove “experiment overlay state”
- Must produce a single undo step: “Accept experiment branch”
Confirm how you will batch the state update into one history entry.

8.3 Edit:
- Must allow reopening the Experiment selector even after conversion.
Question:
- If the origin node is now ‘process’, how will Edit restore the Experiment UI?
  - Option A: temporarily render “Experiment editor popover” for process nodes that have node.meta.experiment
  - Option B: convert node back to experiment temporarily (risky)
Confirm preferred approach.

8.4 Data retention:
- Confirm what data you will preserve in node.meta.experiment:
  - mode
  - selectedOptionId/label/description
  - userPrompt
  - originNodeId
  - generatedNodeIds / generatedEdgeIds (for branch operations)
  - experimentId (explicit)
  - timestamps (generatedAt, acceptedAt)
Confirm exact schema.

--------------------------------------------
9) PRD / SEMANTIC MODEL / EXPORT FILTERING
9.1 Should experiment branches appear in PRD?
For v1:
- YES if accepted
- NO if rejected/preview
Question:
- How will you exclude preview nodes from:
  - extractSemanticWorkflowModel.ts
  - PRD generation
  - exports (kiteframe json / markdown)
Confirm whether you will:
- filter by node.meta.experiment.preview === true
- or maintain a list of preview IDs stored in state

9.2 If a branch is accepted, confirm:
- It becomes indistinguishable from normal workflow nodes except metadata.
- It should be included in PRD generation.

--------------------------------------------
10) MULTI-COLLAB VISIBILITY (YOU CONFIRMED: ALL COLLABORATORS)
10.1 If collaboration is enabled (now or future):
- Are experiment branches visible to all collaborators immediately?
- How do we handle simultaneous edits on the same experiment branch?
Confirm at minimum:
- experimentId is stable
- generated nodes carry experimentId

--------------------------------------------
11) VALIDATION / TESTING QUESTIONS
11.1 Provide 3 test scenarios you will validate:
- Dense canvas near origin (ensure no overlap)
- Two experiments on same origin (ensure headers and branch sets don’t collide)
- Edit after accept (ensure experiment metadata still allows editing)

11.2 Provide instrumentation:
- Add logs for:
  - direction chosen (horizontal/vertical)
  - overlap score
  - experimentId + generated counts
  - accept/reject/edit transitions

--------------------------------------------
12) REQUIRED OUTPUT FROM REPLIT (SO WE CAN WRITE FINAL “DO THIS” INSTRUCTIONS)
Please respond with:
- A short architecture summary of current Experiment implementation (what’s actually working vs placeholders)
- Confirmed file locations + key functions + line numbers
- A proposal for:
  - layout probe utility
  - experimentId grouping strategy for header + drag-all
  - metadata schema for persistence + edit
- A list of blockers or unknowns

END